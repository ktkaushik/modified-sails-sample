var _ = require('underscore'),
    mongoose = require('mongoose'),
    Schema   = mongoose.Schema;

module.exports {

  /**
    *
    * Converts the type object that @waterline provides to 
    * a mongoose object and returns it.
    */
  convertToMongooseType: function( val ) {
    switch (val) {
      case 'String':
      case 'STRING':
      case 'string':
      case 'Str':
      case 'str':
        mongooseObject = Schema.Types.String;
        break;

      case 'Number':
      case 'NUMBER':
      case 'number':
      case 'Num':
      case 'num':
        mongooseObject = Schema.Types.Number;
        break;

      case 'Boolean':
      case 'BOOLEAN':
      case 'boolean':
      case 'Bool':
      case 'bool':
        mongooseObject = Schema.Types.Boolean; // no conditional handlers as of now
        break;

      case 'Array':
      case 'ARRAy':
      case 'array':
      case 'Arr':
      case 'arr':
        mongooseObject = Schema.Types.Array;
        break;

      case 'Date':
      case 'DATE':
      case 'date':
      case 'Dt':
      case 'dt':
        mongooseObject = Schema.Types.Date;
        break;
    }
  }
}